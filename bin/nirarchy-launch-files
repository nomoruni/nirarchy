#!/bin/bash
# nirarchy-launch-files: Open the available file manager

for fm in nautilus dolphin thunar nemo; do
  if command -v "$fm" >/dev/null 2>&1; then
    setsid "$fm" >/dev/null 2>&1 &
    exit 0
  fi
done

xdg-open "$HOME"
